syntax = "proto3";

package streaming;

message Empty {}

message Usuario {
  int32 id = 1;
  string nome = 2;
  int32 idade = 3;
}

message UsuarioList {
  repeated Usuario usuarios = 1;
}

service StreamingService {
  rpc GetUsuarios (Empty) returns (UsuarioList);
}